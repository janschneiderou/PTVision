<UserControl x:Class="PTVision.Grids.PracticeGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PTVision.Grids"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">

        <Button Name="MemoryButton" Click="MemoryButton_Click" MouseEnter="MemoryButton_MouseEnter" MouseLeave="MemoryButton_MouseLeave"  HorizontalAlignment="Left"  BorderThickness="0"  Margin="148,82,0,0" VerticalAlignment="Top" FontSize="20" Background="Transparent" Height="100" Width="100">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Image x:Name="imgMemoryButton" Source="../Images/btn_Memory_E.png" />
        </Button>

        <Button Name="VoiceButton" Click="VoiceButton_Click" MouseEnter="VoiceButton_MouseEnter" MouseLeave="VoiceButton_MouseLeave" HorizontalAlignment="Left"  BorderThickness="0"  Margin="300,155,0,0" VerticalAlignment="Top" FontSize="20" Background="Transparent" Height="100" Width="100">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Image x:Name="imgVoiceButton" Source="../Images/btn_Voice_E.png" />
        </Button>

        <Button Name="PracticeButton" Click="PracticeButton_Click" MouseEnter="PracticeButton_MouseEnter" MouseLeave="PracticeButton_MouseLeave"  HorizontalAlignment="Left"  BorderThickness="0"  Margin="489,225,0,0" VerticalAlignment="Top" FontSize="20" Background="Transparent" Height="100" Width="100">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Image x:Name="imgPracticeButton" Source="../Images/btn_Practice_E.png" />
        </Button>

        <Image x:Name="promoImg" Source="../Images/Promo.png" HorizontalAlignment="Right" Width="200" VerticalAlignment="Top"  Margin="0,0,0,0"  />

    </Grid>
</UserControl>
